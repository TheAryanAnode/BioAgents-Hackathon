# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# HTTP + data
httpx>=0.27,<0.28
networkx>=3.3,<4
numpy>=1.26,<2

# Persistence
SQLAlchemy>=2.0.35,<2.1

# Vector store (optional at runtime; in-memory fallback if unavailable)
chromadb>=0.5.5,<0.6

# PDF ingestion
pypdf>=4.3,<6

# Agent orchestration (optional Nebius LLM at runtime; heuristic fallback if no key)
langgraph>=0.2.28,<0.3
langchain-core>=0.3,<0.4
